{
  "name": "@agentguard/cli",
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "agentguard": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  }
}
